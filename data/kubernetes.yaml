apiVersion: v1
kind: Pod
metadata:
  name: loader
spec:
  containers:
  - name: loader
    image: us.gcr.io/coral-mariner-201700/loader:0.2
    resources:
      limits:
        memory: 2Gi
      requests:
        memory: 2Gi
